trigger TEstTrigger on ContentVersion (before insert) {
  // System.assertEquals(true,false,trigger.new[0].TextPreview);
 // if(trigger.new[0].fileExtension != null)
   // System.assertEquals(true,false,'Hello');
}